language: python
python:
    - 3.5
cache: pip
install:
  - pip install -r requirements.txt
services:
  - postgresql
script:
  - python manage.py test
branches:
  only:
  - master
deploy:
    provider: script
    script: deploy/deploy.sh
    on:
        branch: master
        python: 3.5
        repo: cerny-jan/zpatkydoma"
